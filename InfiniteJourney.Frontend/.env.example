# Web port (nginx)
WEB_PORT=4200

# Runtime config — injected into /assets/app-config.json at container start
API_PORT=5274
KEYCLOAK_URL=http://localhost:8080
KEYCLOAK_REALM=InfiniteJourney
KEYCLOAK_CLIENT_ID=infinite-journey-web
